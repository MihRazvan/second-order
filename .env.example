# Root env example. Each service reads its own .env (see apps/*/.env.example).
# Never commit real keys. On Railway, set these as service variables.
MOBULA_API_KEY=
DATABASE_URL=
NEXT_PUBLIC_STREAM_URL=http://localhost:4010
